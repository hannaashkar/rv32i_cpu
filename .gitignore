# Quartus build artifacts
synth/.mif_prog_stamp
synth/db/
synth/incremental_db/
synth/output_files/
synth/*.log
*.qws
*.qar
*.qarlog
*.rpt
*.summary

# Throwaway STA / characterization projects (D021 Fmax runs etc.) — numbers
# live in docs/DECISIONS.md; the working dirs are disposable.
synth_sta*/

# The two imem bank init images ARE checked in (same policy as
# sw/demo/*.hex): a fresh clone must compile the Quartus project without
# Python or riscv-gcc. Regenerate with `make mif`.

# Simulation build (Verilator runs from the repo root)
obj_dir/
build/
*.vcd
*.fst
*.wlf
transcript
work/

# Software build (but keep checked-in program images like sw/demo)
sw/**/*.o
sw/**/*.elf
sw/**/*.bin
sw/**/*.hex
sw/**/*.map
!sw/demo/*.hex
coremark.log
sw/coremark/.cm_flags_stamp

# MNIST idx files (11.6 MB) — fetched once for scripts/train_mlp.py; see
# docs/NPU.md for the mirror URLs. weights.h (the training OUTPUT) is
# checked in; the raw dataset is not.
sw/npu_mlp/data/

# Generated project book (PDF/HTML) — regenerable artifact, keep out of git history
docs/book/

# Editor / backup noise
*.bak
*.orig
*~

# Session bookkeeping (not project content)
.remember/
obj_dir_ooo/
obj_dir_ooo_cons/
obj_dir_npu/
obj_dir_probe/
obj_dir_stset/
synth/simulation/
ruvector.db
